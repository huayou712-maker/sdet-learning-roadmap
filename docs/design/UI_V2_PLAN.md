# 江湖 UI / Auth 修复设计

从已合并的 main 新建 `fix/jianghu-ui-assets-auth-kaiti`。完整执行根目录 CODEX_UI_FIX_PROMPT.md，素材按 ASSET_MAPPING.md 原样使用，不生成新图片。

设计审核：旧版几何山峰不符合本轮电影感素材要求，已移除；普通米色卡片改成浅纸、砂色和墨褐的层次。仅 Hero 承载强视觉，其他模块以清晰数据和细边界为主。

- 颜色：墨黑 #1f1b15、纸白 #eee2c7、砂色 #c7b48e、赭金 #b58a3b、暗红 #8f382b、玉绿 #526651。
- 字体：全站普通 UI 采用 KaiTi / STKaiti / Kaiti SC / BiauKai / serif；代码和编辑器保留 monospace。不上传字体，设备无楷体时回退系统 serif。
- 构图：66px Header → 电影 Hero → 四项指标 → 路线 / 笔记 / 动态三栏 → 四张项目卡。移动端图片与文字上下排列，账户入口始终在抽屉外。
- 路线概括：Stage 1–3 基础、4–6 测试核心、7–8 工程实战、9–10 进阶；每组按真实知识点数量计算，而不是平均阶段百分比。
- 认证：服务端只向 Header 传入 login、owner、configured；不传 Secret。继续使用现有 signIn/signOut 和服务端身份校验；设置页说明缺失变量名，不显示值。
- 数据：所有学习内容、统计和动态仍来自 GitHub。素材是静态设计资源，不是业务数据；没有增加本地正式存储。

截图使用隔离测试数据，不代表正式学习进度。按参考图人工检查 Hero 可读性、账户入口、四项指标、三栏层次、缩略图及移动端无横向溢出。

人工审查修正：将桌面 Hero 图片放在右侧区域并向左渐隐，保留完整远行者；手机使用 290px 图片与下方独立文字区。头像不可达时显示首字母，键盘焦点使用金色 outline。

验证：29 项单元/组件测试；7 条端到端流程，含原有 CRUD、进度、作业和上传回归，以及 Header、素材、320/390/768/1440px 布局。真实 OAuth 回调仍需配置凭据后验证，本轮不改变认证后端或伪造已配置状态。
