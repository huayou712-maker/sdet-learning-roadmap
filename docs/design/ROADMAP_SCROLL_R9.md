# R9 执行边界：六站山河长卷

## 目标与基线

用户于 2026-09-10 确认：先完成路线页桌面、手机设计稿与短交互演示，再实现单页；独立 PR，全部验收通过后由用户决定合并。

基线：`main` / `254fe687996cf3eb12c7cd685a14ba34d2c3a10d`（PR #8）。分支：`feat/jianghu-roadmap-scroll`。

对象：使用现有六站实践主线学习测试开发的初学者。页面任务：看清路径、打开任一章节、继续已有练习。六站次序、任务 ID、推荐结果、自评及证据语义全部保持原样。

## 视觉方案

- 墨玉 `#142a25`：生图背景基调及图片失败时的纯色底。
- 山青 `#274438`：近景山势与松林，不表示完成状态。
- 青岚 `#608b83`：远山与云海分层。
- 砂金 `#b58a3b`：路线、当前建议任务与交互反馈。
- 月白 `#fff0d1`：图上标题、节点与说明。
- 宣纸 `#f7f3ea`：章节正文，保留清晰无纹理的阅读表面。

字体继续使用已批准的三个角色：展示 `STXingkai / 华文行楷`，正文/控件 `Noto Sans SC / Microsoft YaHei`，序号 `Cascadia Code / Consolas`。本轮不下载或分发新字体，不更换全站排版。

签名元素（生图修订后）：原创云海山水背景承载 SVG 金色路径，将六个真实任务串成一幅可点选的行卷。首次进入时路径有限展开；路径表示目录顺序，绝不表示已掌握或已完成。用户明确否定了初版几何山形，已改用内置 imagegen 的独立背景资源。

```text
桌面  [ 功法长卷 / 原有说明 ]
      [ 题字、入门指南 | 动效开关 ]
      [ 01 ─ 山势 ─ 02 ─ 03 ─ 04 ─ 05 ─ 06 ]
      [ 目录说明 ]
      [ 六个原生可展开章节：交付、验收、知识点、已有记录 ]
      [ 原有十阶段参考目录、自评与证据登记 ]
手机  [ 功法长卷 ] → [ 山势题签 / 开关 ]
      [ 01 → 02 → … → 06 纵向目录 ] → [ 原有章节 ]
```

自审：用六站真实顺序支撑路径，不添加虚构地名、等级、成就或装饰百分比。山河图是唯一高表现区，章节正文无持续运动；保留原生滚动、焦点及深链接。不得为画面更满虚构用户进度。

## 允许修改与硬边界

允许：`components/roadmap/` 的展示组件及局部 CSS、专用展示控制器、相关新增测试、原型与设计文档。按用户后续生图要求增加 `public/images/roadmap/` 背景资源及其来源记录。仅在确有必要时调整路线页展示外壳。

不修改：`data/`、`content/`、学习/推荐/验收算法、API、OAuth、权限、安全校验、GitHub 存储、依赖、CI、生产配置。项目封面和字体可移植性留待后续独立阶段。

GitHub Repository 仍是唯一正式业务数据源。只允许保存非关键 UI 偏好 `sdet-ui-roadmap-motion=on/off`；默认读取不得写入，选中章节只由 URL/内存表达。存储不可用时开关仍在本会话生效。

## 交互与技术约束

1. 六站使用真实锚点链接；保留 `#task-*`、浏览器前进/后退、键盘焦点与原生 details。点选立即展开章节，不等动画结束。
2. 建议继续的任务仍由既有算法提供；“正在查看”只是浏览状态，不能改变或冒充学习进度。
3. SVG 只做装饰并从无障碍树移除，链接为真实 HTML。静态 HTML 中目录与正文可用；不声称既有 Next 流式站点完全无脚本可用。
4. 路径仅一次有限展开，其他效果优先 transform/opacity；无持续 JS 渲染循环、滚动劫持、视频、WebGL、指针尾迹或新动画库。
5. 动效开关覆盖本轮新增效果；系统 reduced-motion 优先。离屏或页面隐藏暂停，卸载清理 observer 和监听。相关 API 不可用时静态降级。
6. 手机采用纵向目录，无缩放/拖拽地图要求；320px、200% 重排宽度和键盘必须可用。链接保持足够触控区域。

## 阶段门禁

- G0：边界文档先提交，确认干净工作树与 main 基线。
- G1：独立 HTML 原型（课程章节标签，无真实学习记录）；保存桌面/手机设计图与短交互录屏，实际查看后再写应用代码。
- G2：实现路线页展示与静态降级；针对性组件测试、原有路线/关联 E2E 和新增动效 E2E 通过。
- G3：`npm run lint`、`npm run typecheck`、`npm run test`、`npm run build`、`npm run test:e2e` 全部通过。保留原 298 项单元测试与 52 项 E2E，不 skip、不删验收、不加大 timeout。
- G4：检查实际桌面/手机画面、录屏与 diff；提交独立 PR 到 main，核对最新提交远端 CI。不自动 merge、不手动发布、不改保护规则。

所有写入验收只使用已有隔离 E2E Adapter/合成数据；截图不算学习成果。权限或凭据不足时报告阻碍，不扩大授权范围。

## 初版执行记录（`c6ab645`，背景修订前）

- G0：已读取 AGENTS.md、Next.js 本地 CSS/客户端边界指南和现有验收，从已合并的 PR #8 建立独立分支。
- G1：已生成并检查 `r9/prototype.html` 的 1440px 桌面、390px 手机设计图及交互录屏。原型不连接 GitHub、不保存学习数据。自审修正：建议任务用金色实心标记，正在查看用独立外圈与文字，避免浏览状态被误认成学习进度。
- G2：已将独立 SVG 山河图接入原有六章目录，章节正文、知识点、关联记录与推荐计算保留原样。新增 12 项单元测试；原有路线/关联 E2E 5/5、新增路线动效 E2E 7/7 通过。系统减少动态效果、手动关闭、存储异常、观察 API 缺失、离屏/后台暂停及卸载清理均有验收。
- G3：最终生产实现的 lint、typecheck、310 项单元测试、build 与全量 59 项 E2E 全部通过（原有 298 + 新增 12；原有 52 + 新增 7）。未改动任何原有测试、依赖、CI 或超时。全量 E2E 用时约 5.8 分钟，截图与写入只涉及隔离演示数据。
- G4 本地验稿：检查了 1440px 桌面、390px 手机实际页面及交互录屏。七档宽度覆盖 320、390、720、900、1024、1440、1920px，入口至少 44px，无横向溢出；手机推荐提示独立分行。真实鼠标点击可在路径动画尚未结束时完成选章；展开不会写入学习进度。远端 PR/CI 状态在提交后单独核对，不提前标记完成。

## 生图修订验收（2026-09-10）

- 按用户要求使用内置 imagegen，替换初版几何山形。保存 1774 × 887 原始 PNG，网页资源为相同尺寸的 WebP（286,188 字节），不放大冒充更高分辨率。
- 图片只做装饰；金色路径、题字、六个真实章节链接及动效开关仍由界面渲染。手机使用右侧山峰裁切，文字保留独立遮罩及底色。图片失败仍能选章。
- 本轮新增 2 项单元测试、1 项 E2E，既有验收全部保留。最终 lint、typecheck、312 项单元测试、build、60 项 E2E 全部通过，全量 E2E 约 6.1 分钟。
- 已检查当前桌面和手机截图，并更新交互演示。沿用原分支，未合并或手动部署；核对时尚无开放 PR，远端 CI 不能标记通过。

## 交付材料

- 初版过程记录（几何山形方案已替换）：[交互原型](r9/prototype.html)、[桌面设计稿](r9/desktop.png)、[手机设计稿](r9/mobile.png)、[原型交互演示](r9/prototype-interaction.webm)。
- [实现后的桌面山河图](r9/implemented-desktop.png) · [实现后的手机目录](r9/implemented-mobile.png) · [真实浏览器交互录屏](r9/implemented-interaction.webm)
- [生图背景、原图与最终提示词](r9/BACKGROUND_IMAGEGEN.md)

画面中的课程来自既有六站任务，录屏中的学习状态来自隔离 E2E 环境，不是个人学习成果。展示字体沿用本机已批准字体栈，未添加字体下载或远程图片。

截图沿用项目既有 `caret: initial` 安全选项，以有界轮询等待字体，并按文档坐标采集完整目录；当前版本另外等待实际需要的背景图片加载。全量日志中保留了颜色环境变量、旧图片 LCP 提示及页面切换取消流等输出，没有通过屏蔽日志或绕过测试宣称无错误。
